"""
座標常數集中管理
================
所有座標都以 1920x1080 為基準畫布，實際點擊時由 FGOBot._to_tap() 換算。

⚠️ 遊戲改版導致 UI 位移時，只要改這一個檔案即可，
   不必再到 fgo_combat / fgo_vision / fgo_nav 三處分別修改。

索引慣例：凡是對應「指令編號」的清單，索引 0 一律放 None，
         讓 SKILLS[1] 就是技能 1，避免 +1 -1 的換算錯誤。
"""

# ==========================================
# ⚔️ 戰鬥畫面
# ==========================================

# 從者技能：3 名從者 x 3 個技能，由左至右
SKILLS = [
    None,
    (111, 871), (244, 871), (376, 871),      # 從者 1
    (586, 871), (716, 871), (847, 871),      # 從者 2
    (1060, 871), (1191, 871), (1324, 871),   # 從者 3
]

# 御主技能（迦勒底戰鬥服等禮裝的 3 個技能）
MASTER_SKILLS = [None, (1360, 468), (1493, 468), (1626, 468)]

# 御主技能開關按鈕。展開時再點一次即可收合
MASTER_BTN = (1792, 472)

# 技能施放後的「選擇對象」目標（我方 3 名從者）
TARGETS = [None, (485, 590), (965, 590), (1425, 590)]

# 敵方切換（畫面左上角的敵人頭像）
ENEMIES = [None, (65, 65), (440, 65), (815, 65)]

# 寶具卡（按下 Attack 之後才會出現）
NP_CARDS = [None, (605, 300), (960, 300), (1315, 300)]

# 指令卡 5 張，由左至右
CARDS = [(195, 755), (580, 755), (960, 755), (1345, 755), (1735, 755)]

# Attack 按鈕
ATTACK_BTN = (1650, 920)

# 空白處：用來連點跳過動畫。選在不會誤觸任何按鈕的位置
BLANK_SPOT = (1750, 150)

# 長按從者開啟詳情用（強制洗白 UI 時使用）
SERVANT_PORTRAIT = (705, 670)

# ==========================================
# 🔄 換人（Order Change）
# ==========================================
ORDER_FRONT = [None, (205, 520), (500, 520), (805, 520)]    # 場上 3 人
ORDER_BACK = [None, (1095, 520), (1400, 520), (1695, 520)]  # 後備 3 人
ORDER_CONFIRM_BTN = (963, 940)

# 換人功能實際佔用的是「第 3 個御主技能」的位置
ORDER_CHANGE_SKILL = MASTER_SKILLS[3]

# ==========================================
# 🎯 全自動 AI 模式的目標嘗試順序
# ==========================================
# 比 TARGETS 多兩個位置，用來涵蓋不同的敵人排列，逐一嘗試直到視窗關閉
AI_TARGET_POSITIONS = [
    (485, 590), (725, 590), (965, 590), (1195, 590), (1425, 590)
]

# ==========================================
# 🧭 選單與導覽
# ==========================================
BACK_BTN = (65, 65)               # 左上角返回／盲點推進結算
AP_DECIDE_FALLBACK = (1280, 830)  # 吃蘋果確認鈕的盲點位置
QUEST_START_FALLBACK = (1750, 1000)   # 找不到出擊按鈕時的盲點位置
CONTINUE_BATTLE_FALLBACK = (650, 850) # 結算頁「連續戰鬥」的盲點位置
REJECT_FRIEND_FALLBACK = (480, 850)   # 好友申請「拒絕」的盲點位置
INTERLUDE_QUEST = (1380, 300)     # 大廳選擇任務的位置
SUPPORT_BLIND_PICK = (600, 350)   # 助戰清單第一位（盲選用）

# 隊伍切換圓點：15 隊平均分布在 first 與 last 之間
TEAM_DOT_FIRST_X = 697
TEAM_DOT_LAST_X = 1222
TEAM_DOT_Y = 75
TEAM_DOT_COUNT = 15

def team_dot_x(team_index):
    """回傳第 N 隊圓點的 x 座標（team_index 從 1 開始）"""
    gap = (TEAM_DOT_LAST_X - TEAM_DOT_FIRST_X) / float(TEAM_DOT_COUNT - 1)
    return int(TEAM_DOT_FIRST_X + (team_index - 1) * gap)

# ==========================================
# 📖 劇情
# ==========================================
SKIP_BTN = (1798, 61)
# 劇情選項：SKIP 呈暗色時代表卡在選項，依序盲點這三個位置
STORY_OPTIONS = [(950, 315), (950, 415), (950, 505)]

# ==========================================
# 👁️ 視覺辨識用的搜尋範圍 (x1, y1, x2, y2)
# ==========================================
ROI_SKIP_BTN = (1500, 0, 1920, 150)   # SKIP 按鈕只可能出現在右上角

# 技能 CD 數字相對於技能圖示中心的偏移量
CD_ROI_OFFSET_X = 55   # 左右各取這麼寬
CD_ROI_OFFSET_Y = 60   # 從圖示中心往下取這麼高