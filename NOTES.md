# 設計決策紀錄

記錄「為什麼這樣做」以及「評估過但決定不做」的事情。

程式碼能說明「做了什麼」，但說不清楚「當初考慮過哪些選項、為什麼選這個」。
沒有這份紀錄的話，未來很可能重新踩一次同樣的坑 ——
例如看到某段程式碼覺得「這裡加個判斷可以省 5 秒」，然後重蹈覆轍。

---

## 效能

### 瓶頸在截圖，不在影像比對

實測一場周回的 ADB 耗時（v1.1.0 時期）：

| 項目 | 次數 | 平均 | 總計 | 佔比 |
|---|---|---|---|---|
| 截圖 | 108 | 744ms | 80.3s | 73% |
| 點擊 | 190 | 152ms | 28.9s | 26% |

**當初的直覺（點擊太慢）是錯的**，所以先做了量測才動手。
`fgo_core.ADB_PROFILE = True` 可以重現這份統計。

而加了 ROI 之後，OpenCV 的模板比對只佔每圈約 5%，遠遠不是瓶頸。
「OpenCV 效能榨乾了」是誤解。

### raw 截圖（已採用）

`exec-out screencap` 不加 `-p`，直接取原始 RGBA，省掉裝置端的 PNG 壓縮與本機解壓。
實測 744ms → 368ms。

**風險與對策**：raw 最危險的失敗模式不是報錯，而是「解析成功但色彩通道順序錯誤」——
程式不會發現，只會讓所有模板比對失敗，症狀是腳本什麼都不做。
因此加了自我驗證：第一次截圖後立刻抓一張 PNG 對照色差，不符就永久退回 PNG 模式。

### ROI 搜尋範圍（已採用）

限定模板的搜尋範圍，平均加速約 7 倍。範圍是由 `ROI_RECORD` 收集實際命中位置後產生，
並向外放寬 200px（橫）/ 150px（縱），不是用猜的。

**自我修復機制**：ROI 內找不到時，每累積 100 次未命中才做一次全畫面複查。
若複查發現圖其實在畫面上（代表遊戲改版位移），就印警告並自動停用該圖的 ROI。
最壞情況只是退回原速，不會靜默失效。

不採用「未命中就立刻退回全畫面」，因為大多數掃描本來就是未命中，那樣會讓 ROI 完全失去意義。

### input tap 取代 input swipe（已採用）

原本用 `input swipe x y x y duration` 模擬點擊以控制按壓時間。
改用 `input tap` 後平均 152ms → 75ms，一場約省 15 秒。

保留 `LONG_PRESS_MS` 門檻：按壓時間超過該值仍走 swipe，替未來可能的長按需求留後路。
UI 上做成「快速點擊」開關，遇到漏點可關閉。

### ❌ ADB 常駐連線（評估後不做）

構想是開一個長期存活的 `adb shell`，用 stdin 持續送指令，省下每次啟動行程的開銷。

**不做的理由**：實測點擊平均 75ms，其中大部分是刻意的按壓時間，
真正的固定開銷約 60ms。一場 213 次點擊最多省 13 秒。

而代價是：連線狀態要自己管理（斷線不會立即察覺）、回應要自己切割、
截圖是二進位資料難以走同一條管線、多執行緒要加鎖、程式關閉要確保子行程被殺掉。
**風險與收益不成比例。**

### ❌ 預期式掃描（評估後不做）

構想是狀態機根據上一步預測下一個畫面，只掃那幾張圖而不是全套掃描。

**不做的理由**：預期落空的情況太多（AP 不足、素材上限、活動公告、網路延遲…），
每一種都要事先想到並寫例外處理。想漏一個就會盯著永遠不會出現的圖卡死，
而且這種 bug 只在特定情境重現，可能掛機三天才遇到一次。

### ❌ 換掉截圖管道（minicap / scrcpy / 模擬器原生 API，暫緩）

理論上能讓截圖成本趨近於零。但要處理二進位協定、版本相容、模擬器綁定，
而且原生 API 會犧牲通用性。

更重要的是：一場周回的時間大宗是**動畫**，那是遊戲決定的，砍不掉。
就算截圖免費，大概也只能再省 30~40 秒。投入產出比不高。

---

## 判斷邏輯

### CD 判斷維持「逐一即時判斷」

`fgo_vision.check_skill_available_batch()` 可以用一張圖判斷全部 9 個技能，
一波能省約 1.5 秒。**但只用在 AI 隨機模式，腳本模式刻意不用。**

**不用的理由**：隊伍中常有「A 角色幫 B 角色減冷卻」的組合，
回合開始時的快照會說「B 還在 CD」而跳過，但實際上放得出來。
換人也會讓整組技能換掉，快照直接失效。

這不是快慢問題，是**會漏放技能**，所以選擇較慢但正確的做法。

### CD 判斷的門檻來自實測

`cooldown_text_tpl.png` 是「剩餘」兩字的模板。實測分數分布：

- 冷卻中：0.809 ~ 0.825
- 可使用：0.686 ~ 0.714

門檻 0.75 落在中間，兩側都有餘裕。
`fgo_vision.CD_DEBUG = True` 可以重新收集樣本。

曾經懷疑「判斷不準」是門檻問題，實際上是 `TM_CCORR_NORMED` 搭配遮罩會產生 NaN，
而 `NaN >= 0.75` 為 False，被誤判成「沒有 CD」。已加上 NaN 防護。

也曾嘗試改用亮度或「亮白像素比例」判斷，但實測顯示亮白像素完全重疊，不可行。
搜尋範圍也試過裁小（只取「剩餘」二字所在的左下角），分離度反而變差 ——
真正的訊號是**整個圖示變暗**，範圍越大越穩定。

### 換人改用 SELECT 標記判斷

原本靠確定鈕的亮度判斷「選滿了沒」，但那只知道不夠，不知道缺哪一邊，
所以重試邏輯是試探性的。而且**選錯人時確定鈕照樣會亮**，程式察覺不到。

改用畫面上的 SELECT 標記後，可以直接知道前後排各選了誰，重試變成確定性的。
歸屬方式是只比對 x 座標（六格橫向間距約 295px，不會認錯），
配合 `ORDER_SELECT_Y` 排除畫面別處的誤判。

舊的亮度判斷保留為後備，`order_select.png` 不存在時自動退回。

### ❌ 用 order_change_btn.png 判斷御主技能欄是否展開（試過，不可行）

構想是讓「啟動時的 UI 洗白」變聰明：畫面乾淨就跳過，省 5 秒。

**不可行的理由**：換人技能進 CD 後外觀會變（變暗、疊上「剩餘」字樣），模板比對失敗；
而且不是每個御主禮裝都有換人技能。

改為**一律執行洗白**。這個動作整個腳本執行期間只跑一次，
用 5 秒的固定成本換「絕不誤判」，對長時間掛機划算得多。

---

## 腳本檢查器

### 設計原則：零設定成本

不要求使用者填任何資料（例如各技能的冷卻回合數）。

**理由**：一個需要先填九個欄位才能用的檢查器，等於把「防止設定錯誤」的成本
換成「設定更多東西」，對新手是淨損失。而且填錯會讓檢查結果反過來誤導人。

代價是某些判斷只能給提醒而無法斷定，這是刻意的取捨。

### 嚴重度的分級標準

- 🔴 **錯誤**：結構上必然失敗（順序錯誤、編號超範圍、重複選寶具、換人兩次）
- 🟡 **警告**：多數情況是錯的，但存在合理例外
- 🔵 **提醒**：需要搭配特定隊伍或效果才成立

**曾經把「技能重複使用」列為警告，後來降級成提醒。**
原因是紅卡隊常刻意用減 CD 技能讓同一個技能再放一次，
列成警告會讓正常配置滿江黃燈 —— 一個在正常情況下就狂跳警告的檢查器，
比沒有還糟，因為它會訓練使用者忽略所有警告。

### 已知的簡化

`MAX_ENEMIES = 3`。少數關卡有 4~6 隻敵人，但周回關卡多為 1~3 隻，
目前 UI 與座標表也只支援三隻。要放寬需先量測第 4~6 個敵人頭像的座標。

---

## 架構

### 座標集中在 coords.py

同一組技能座標原本散在 `fgo_vision`、`fgo_combat` 三處，且**索引基準還不一致**
（一邊有前導 None、一邊沒有）。遊戲改版時要改三個地方，漏掉的那個不會報錯，
只會點到空白處。

現在統一由 `coords.py` 提供，索引慣例是「索引 0 一律放 None」，
讓 `SKILLS[1]` 就是技能 1。

### find_in_folder 的 click_it 預設為 False

這個 API 同時能「找」和「點」。預設為 True 時，忘記寫參數的後果是
**多點一下**（不會報錯，但會誤觸畫面，很難查）；
預設為 False 則是**少點一下**（畫面卡住，一眼看得出來）。

已把所有需要點擊的呼叫改為明示 `click_it=True`。

### 日誌接管 sys.stdout 而非改寫 print

專案裡有數百個 `print`。與其逐一改寫，不如接管 `sys.stdout` / `sys.stderr`，
讓既有的 print 自動同時寫入檔案 —— 零風險，也不會漏掉任何一行。

同時掛上 `sys.excepthook` 與 `threading.excepthook`：
腳本邏輯跑在背景執行緒，Python 預設**不會**把子執行緒的例外送到 `sys.excepthook`，
沒有這個的話執行緒無聲死掉會完全查不到原因。

### UI 清單元件重複使用

指令清單原本每次更新都全部銷毀重建，導致上下移動時畫面閃爍。
改成重複使用元件、只更新文字與狀態後，移動時完全不建立或銷毀元件。

一般原則：**更新頻率低就砍掉重建（簡單可靠），更新頻率高才做重用（複雜但流暢）**。

### ADB 伺服器是全機器共用的

`adb devices` 問的不是模擬器，是 **adb 伺服器自己的記帳本**：

```
程式 → adb.exe(客戶端) → adb server(常駐, port 5037) → 模擬器
                              ↑ devices 清單來自這裡
```

`adb connect` 成功後 server 會把連線記進表裡，但**模擬器被關掉時那條 socket 不會立刻通知
server** —— 要等下一次寫入失敗或逾時才會轉成 offline。這個空窗期內 `adb devices` 會
很有自信地回報 `device`，這就是「幽靈連線」。第二個來源是 `detect_devices` 會盲目地對
8 個常見埠 connect，可能留下半開的紀錄。

所以 `_verify()` 不看記帳本，直接送一次 `wm size` 看對方會不會回話。這是唯一可靠的判斷。

**內建 adb.exe 解決不了這件事。** 它保證的是「哪個客戶端執行檔」，而 adb server 是整台
機器共用一份：若模擬器已經啟動了版本相同的 server，我們的 adb.exe 會直接接手用它，
連同它的記帳本與幽靈連線一起繼承；版本不同時則會殺掉重啟（`adb server version doesn't
match this client; killing...`），這又會讓使用者的 scrcpy / Android Studio 突然斷線。

內建 adb 真正的價值是另外三件事：使用者不必安裝 Android SDK 或設 PATH、行為可預期
（回報問題時少一個變因）、輸出格式固定。**「用哪個工具」與「連到哪台裝置」是兩個獨立
的問題，內建 adb 管前者，`_verify` 管後者，不能互相取代。**

### 一台模擬器會佔用多個連接埠

實測（MuMu 12，兩個實例同時開啟）：

| 連接埠 | android_id | 螢幕 | 機型 |
|---|---|---|---|
| 16384 | `ff1ebdb6b57bc3fa` | 1080x1920 | ASUSAI2501B |
| 5555 | `ff1ebdb6b57bc3fa` | 1080x1920 | ASUSAI2501B |
| 7555 | `ff1ebdb6b57bc3fa` | 1080x1920 | ASUSAI2501B |
| 16416 | `a6cbc2b541b9a569` | 900x1600 | SM-A536E |

**同一台 MuMu 同時在 16384 / 5555 / 7555 三個埠上聽**，三者通往同一個實例；
第二個實例才是 16416。所以 `adb devices` 列出 4 筆時，實際只有 2 台。

`_verify()` 因此除了驗活，也負責合併：**`android_id` 是可靠的辨識鍵**（同實例相同、
跨實例不同），而且可以跟 `wm size` 併在同一次 shell 呼叫取回，合併等於免費。
取不到 android_id 時（回 null 或空）刻意不合併 —— 寧可多列一筆，也不要把兩台真的
模擬器誤併成一台。

### 找多開的模擬器要先用 socket 探測，不能直接 adb connect

多開的第二、三台**不會自己註冊到 adb**，必須有人執行 `adb connect` 才看得到。
而 adb 伺服器是常駐行程，一旦連上就會一直記著，跨越本程式的重啟 ——
所以會出現「重開 app 就看得到第二台」的錯覺，其實是伺服器裡留著先前的連線。

原本 `detect_devices` 只在裝置清單全空時才掃埠，第一台一出現就短路，
等於永遠找不到其他台。不直接改成「每次都掃」的原因是成本：

| 方式 | 單一空埠 | 掃 16 個埠 |
|---|---|---|
| `adb connect` | 2430 ms | 19 秒以上 |
| socket 探測（逐一） | 65 ms | 1044 ms |
| socket 探測（並行） | — | **175 ms** |

改成「先用 socket 篩出有在聽的埠，只對那些做 adb connect」之後，整份清單約 0.2 秒掃完，
便宜到可以每次偵測都跑。有在聽的埠實測 11~13ms 就連上，逾時設 50ms 有 4 倍餘裕。

探測用的 `PROBE_PORTS` 與 `COMMON_ADB_PORTS` 刻意分成兩份：後者是給後備路徑用
`adb connect` 逐一嘗試的，每多一個埠就多 2.4 秒，不能跟著擴充。

**先做去重再擴大掃描範圍是有順序的**：掃得越廣，同一台模擬器的重複埠就越多
（實測 5557 也在聽），沒有 `_verify` 的合併會讓清單爆量。

**連上的別名要自己收乾淨。** 每次 `adb connect` 成功，adb 伺服器就會永久記住那條連線。
實測連續偵測幾次之後，`adb devices` 從 2 筆長到 7 筆（16384 / 16416 / 5555 / 5557 /
7555 / emulator-5554 / emulator-5556）—— 不只汙染機器上其他工具看到的清單，
也讓往後每一次驗證都要多問好幾台。所以 `_disconnect_aliases()` 會把「這一輪自己連上、
事後確認是重複別名」的斷開；原本就在清單裡的一律不碰，那可能是使用者或其他工具建立的。

但光是斷開還不夠：下次偵測又會把它們重連一次再斷掉，白花約 1.5 秒。因此用 `_alias_ports`
記住學到的別名埠，之後直接跳過。**這份快取會在偵測結果的裝置組合改變時整個丟掉**——
因為別名歸屬不是固定的（見上），某個別名埠日後可能變成另一台模擬器的主要埠，
不重新學就會漏掉那台。

實測效果：第一次偵測 3.0 秒（學習 + 清理），第二次起穩定 1.35 秒，`adb devices` 不再累積。

### ❌ 用 ANDROID_ADB_SERVER_PORT 隔離出專屬伺服器（評估後不做）

構想是讓程式跑在自己的 port（例如 5038），完全不跟模擬器 / Android Studio 共用 server。
這樣幽靈連線的來源少一大半，也不會互相殺 server。

**不做的理由**：模擬器自己 connect 上去的裝置會完全看不到，等於每次都得自己重新 connect；
多開偵測要整個重寫；使用者想用其他工具排查時也看不到我們的裝置，反而更難查問題。

跟「ADB 常駐連線」是同一類的取捨：理論上更乾淨，但風險與收益不成比例。
目前 `_verify` 已經用很低的成本處理掉實際會遇到的症狀了。

### 自動更新的限制

更新用的批次檔是由**舊版本**產生的，所以「修更新程式的修正」本身
也要等下一次更新才會生效。

v1.2.1 加入了更新前先 `taskkill adb.exe`（常駐的 ADB 服務會鎖住 platform-tools 內的檔案），
但從 1.2.0 以前的版本更新時仍會遇到該問題。

robocopy 不使用 `/PURGE` 或 `/MIR`，並以 `/XD` `/XF` 排除
`assets_saves`、`assets_friends`、`config.json`，確保使用者資料不被覆蓋。

---

## 待辦與構想

依「價值 ÷ 工作量」排序：

1. **NP% 與血量辨識** —— 目前程式只知道「卡片是紅的」，不知道 NP 條、血量、buff。
   沒有這些資訊，再聰明的決策邏輯也只能瞎猜；有了之後，即使只用簡單規則也能打得很好。
   **這是「導入 AI」真正的前置條件**，瓶頸在狀態擷取而不在決策演算法。
2. **敵方 4~6 隻支援** —— 需先量測座標。
3. **換人技能格數設錯的防護**（規格已定，等實際需要再做）——
   目前兩件有換人的御主禮裝都把它放在第 3 格，UI 已可設定 1~3。
   但使用者若設錯格數，程式會去點一個不相干的御主技能，接著
   `_do_order_change` 會在沒有面板的情況下盲點 `ORDER_FRONT` / `ORDER_BACK`，
   最多在戰鬥畫面上亂點六下 —— 設定錯誤的代價從「換人沒成功」被放大成「亂點」。

   已確定的做法：
   - 點下換人技能後、進 `_do_order_change` 之前，先確認 `order_change_confirm_btn.png`
     在畫面上。實測確認該按鈕在面板開啟期間一直存在，暗色時模板比對仍找得到，
     所以可以直接當「面板開了沒」的指標，不需要新素材。
   - 沒開的話分三種情況脫困：跳出選擇對象視窗（`select_target_text.png`）→
     點對象 1 讓技能落地再等動畫（視窗不處理掉會讓後面所有指令失效）；
     技能已直接施放 → 等動畫結束；技能列還開著 → 點 `master_btn` 收合。
   - 連續失敗 3 次才停止腳本，單次抖動不中斷掛機。
   - 順手把 `oc_slot` 的邊界檢查搬到開技能列之前，與 M 指令「非法指令完全不碰 UI」一致。
     （單獨做沒有價值：oc_slot 來自鎖死 1~3 的下拉選單，`_safe_pos` 永遠會通過。）

   觸發條件：出現換人技能不在第 3 格的御主禮裝。在那之前程式走的路徑與現在完全相同。

4. **多台模擬器同時運作** —— 讓 A 台跑一份設定、B 台跑另一份，各自獨立作業。

   **架構上可行，而且底層幾乎已經準備好了**：
   - `FGOBot` 建構時收一個 `device_id`，所有 adb 指令都是 `-s <device_id>`
     （`fgo_core.py` 的 `adb_shell` 是唯一出口），本來就只會操作一台
   - `FGOLogic` 已經是自足單位：自己的 bot、自己的 config、自己的狀態機
   - vision / combat / nav 全部透過 `self.ctx` 取用，沒有模組層級的可變狀態
   - `FGOBot` 的 `_roi_miss`、`_roi_disabled`、`template_cache` 都是實例層級
   - `run_logic()` 本來就跑在獨立執行緒

   所以「開 N 個 FGOLogic、各給不同 device_id 與 config、各跑一條執行緒」在核心層
   幾乎不用改。真正的工作在外圍：

   1. **日誌無法分辨來源（最麻煩）**。`fgo_logger` 接管的是全域 `sys.stdout`，
      N 台的 print 會交錯在同一份檔案，排查問題會非常痛苦。可行解是讓 `_Tee`
      查一個 thread-local 的裝置標記再加前綴 —— 因為每台本來就在自己的執行緒裡，
      這個對應關係是天然成立的。
   2. **UI 是單例**。`start_thread` 只建一個 FGOLogic、`logic_thread` 是單一參照、
      `session_id` 是單一計數器、狀態列只有一行、整份設定只有一組。
      這是 `main_ui.py` 的大改。
   3. **設定要能綁定裝置**。目前 `config.json` 是單一份。要 A 台用隊伍 1 + 腳本甲、
      B 台用隊伍 2 + 腳本乙，需要「執行單元 = (device_id, 存檔名稱)」的概念。
   4. `fgo_core._adb_timings` 是模組層級全域 dict，多台統計會混在一起
      （只影響 ADB_PROFILE 診斷，不致命）。

   **建議的最小可行形態：先不要改 UI，改成能跑多個 process。**
   因為 FGOLogic 已經自足，跑兩個獨立行程幾乎不用改任何程式碼，各自有各自的
   設定與紀錄檔，也天然避開上面第 1、2 點。唯一的衝突是同一個資料夾共用
   `config.json`，加一個命令列參數指定設定檔位置就能解決。
   等真的確認有人要用，再考慮值不值得做成單一視窗管理多台。

   **可能的真瓶頸不在程式**：兩個 Android 模擬器同時跑 FGO 對一般電腦的負擔，
   遠大於腳本本身。截圖佔耗時 73%（那是等待 ADB 的 IO，多台可以重疊），
   但 OpenCV 比對與模擬器本身的 CPU 佔用會累加。要做之前先確認硬體撐得住。

5. **離線重放框架** —— 讓 `capture_screen` 可以從資料夾讀圖，
   配合帶答案的測試畫面庫，調參數不用開模擬器。維護成本較高，暫緩。
6. **介面客製化**（圖片按鈕、背景圖）—— 素材流程已有雛形（`testsss.py`），
   但屬於換皮，等底層穩定再做。
7. **LLM 產生腳本** —— 使用者描述隊伍、由 LLM 產出 3T 腳本。
   離線執行、允許慢、錯了可重來，比即時決策務實得多。
8. ❌ **帳號系統** —— 除非確認有需求否則不做。
   「分享腳本」可用「設定編碼成文字字串」達成，零伺服器、零隱私責任。
